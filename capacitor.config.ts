import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.material.idle.shards',
  appName: 'Material Idle Shards',
  webDir: 'dist'
};

export default config;
